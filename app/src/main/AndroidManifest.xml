<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jan.t9_mobileapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Bolzer"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
        The API key for Google Maps-based APIs is defined as a string resource.
        (See the file "res/values/google_maps_api.xml").
        Note that the API key is linked to the encryption key used to sign the APK.
        You need a different API key for each encryption key, including the release key that is used to
        sign the APK for publishing.
        You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".Homescreen">
        </activity>
        <activity
            android:name=".Profile"
            android:parentActivityName=".Homescreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homescreen" />
        </activity>
        <activity
            android:name=".Team"
            android:parentActivityName=".BunteLiga">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".BunteLiga" />
        </activity>
        <activity
            android:name=".EventRadar"
            android:parentActivityName=".Homescreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homescreen" />
        </activity>
        <activity
            android:name=".BunteLiga"
            android:parentActivityName=".Homescreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homescreen" />
        </activity>
        <activity
            android:name=".Search"
            android:parentActivityName=".Homescreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Homescreen" />
        </activity>
        <activity
            android:name=".CreateEvent"
            android:parentActivityName=".EventRadar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".EventRadar" />
        </activity>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>